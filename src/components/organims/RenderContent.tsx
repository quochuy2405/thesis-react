

const RenderContent: React.FC= () => {

	return (
		<>
			{/* {(params.get("tab") === "all" || !params.get("tab")) && props?.social}
			{params.get("tab") === "manage" && props?.manage}
			{params.get("tab") === "private" && props?.private}
			{params.get("tab") === "search" && props?.search}
			{params.get("tab") === "trash" && props?.trash}
			{params.get("tab") === "analysis" && props?.analysis} */}
		</>
	);
};

export default RenderContent;
